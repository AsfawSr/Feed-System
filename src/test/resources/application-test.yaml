spring:
  datasource:
    url: jdbc:h2:mem:feed_system_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: localhost
      port: 6379

feedsystem:
  jwt:
    secret: test-secret-that-is-at-least-32-bytes-long
